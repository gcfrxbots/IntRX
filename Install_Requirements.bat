echo ================================================================= & echo.You need to have Python 3.7 (and the included PIP package) installed for this to work!
pause

py -3.7 -m pip install -r requirements.txt --user --no-warn-script-location

pause